// use this baseUrl approach for production (with different host value though)
// const protocol = 'http';
// const host = '127.0.0.1';
// const port = 3000;
// export const baseUrl = `${protocol}://${host}:${port}`;

// Use this baseUrl for dev 
export const baseUrl = '/api'; // use proxy setup in vite.config.js
