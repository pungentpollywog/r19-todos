# Setup

## Dependencies

This upgrades express, mongoose, cors and installs the other dependencies needed (bcrypt, jsonwebtoken, and passport strategies.)
```
npm i express@latest mongoose@latest cors@latest bcrypt@latest jsonwebtoken@latest passport@latest passport-jwt@latest passport-local@latest 
```

Add linting capabilities 
```
npm i @eslint/js@latest eslint@latest
```